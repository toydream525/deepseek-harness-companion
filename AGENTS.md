# 工作约定

## 持续协调与交付

- 主代理负责方案、任务分派、接口协调、结果汇总和最终审核；实施与实际验证交由 GPT-6 Sol，轻量独立工作可交 GPT-6 Luna。思考强度按任务难度选择。
- 用户已明确要求持续推进：普通进度汇报不代表任务结束，不要在汇报后停止工作等待用户再次说“继续”。持续完成当前已授权任务，直至交付和发布完成，或出现确实需要用户决定的阻塞。
- 子代理完成后主动向主代理交回实际结果、文件位置、验证范围和剩余问题；主代理及时接手下一步，不让用户中转消息。
- 有可独立推进的工作时继续执行。需要用户输入时只暂停依赖该输入的部分，不将沉默当作批准。
- 不因例行步骤反复请求已获得的授权。公开发布按当前会话明确授权的仓库、域名及范围执行，不能扩大授权范围。

## 审核与效率

- 优先完成必要修复、受影响功能验证和发布，不做重复检查、无关润色或无依据扩大测试范围。
- 真实操作和结果是功能验收依据；哈希仅用于工件身份与完整性，不能代替功能测试。发布校验值和后台完整性校验保留。
- 区分源码验证、候选安装包、最终安装包以及用户当前运行版本；未验证的范围必须明确。
- 实施完成后统一审核，发现具体问题再修复并做针对性验证。
- 不覆盖正在运行的程序，不擅自停止用户当前使用的模型或外部 DSH；遵守会话中已有的具体启停授权。
- 不把个人配置、密钥、认证链接、聊天记录、模型权重或未授权第三方程序放入公开源码或发布包。
